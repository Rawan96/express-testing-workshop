const request = require('supertest');

const app = require('../app');

//Fill this with many many tests YAY!! 😜😩

//The first test
test("Initial Test", () => {
  const num = 2;
  expect(num).toBe(2);
});